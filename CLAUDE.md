# preview-build

Read [README.md](README.md) before deploying to this repo. It defines the
deployment rules: one stable path per branch, never share a preview link until
`build-id.txt` reports the expected SHA, and cache correctness belongs to the
app build rather than to this host.
